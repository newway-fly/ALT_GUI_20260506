注意：需要保证GUI不卡顿不阻塞的基础，所有动作加入任务队列且由任务队列来驱动，串口基本就是只发送信息和接收信息，所有数据处理(处理数据格式或者画图等等)都是在后台由任务队列指派执行甚至长耗时要丢进去线程来处理。
注意：优化函数时，不允许在函数中又def一个新函数的这种写法。且代码优化过程不能过度扩展，严格按照我的需求来。
对需要修改到的文件要给出详细完整的程序和指明位置，并且必须有详细的注释，便于复制直接使用和读懂

对现有的问题进行处理，但是要遵循现有的代码框架和代码基础，做尽量合理且小幅度的代码改动优化。
注意：优化函数时，不允许在函数中又def一个新函数的这种写法。且代码优化方向可以提建议，但是不要过度扩展，只按我的需求来。
对需要修改到的代码块要给出详细且完整的代码块和指明代码块所在文件和类下面的位置，便于直接复制使用；必须有详细的注释易于读懂。

pip install pyvisa -i https://pypi.tuna.tsinghua.edu.cn/simple
pip install pyserial -i https://pypi.tuna.tsinghua.edu.cn/simple
pip install pip install PyQt5-tools pyqt5-plugins -i https://pypi.tuna.tsinghua.edu.cn/simple
pip install pyqtgraph -i https://pypi.tuna.tsinghua.edu.cn/simple


deactivate
Remove-Item -Recurse -Force .venv    强制删除虚拟环境
快捷键:F1,然后安装虚拟环境
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
& .venv\Scripts\Activate.ps1
pip freeze > requirements.txt



pyinstaller --onefile main.py -n AFR_Equipment_Control -i star.png

pyinstaller --version-file version_info.txt -F -w main.py -n GUI_DR4_V0.1
pyinstaller --version-file ./version_info/version_info.txt -F -w main.py -n GUI_DR4_V0.2

ALT_GUI_20260208_buf/
│
├── main.py                       # 程序入口
├── requirements.txt              # 依赖库列表
├── 说明.ini                      # 说明文件
│
├── core/                         # 核心逻辑层
│   ├── __init__.py
│   ├── logger.py                 # 日志模块
│   ├── protocol.py               # 协议定义 (CmdType, Pack, Parse)
│   ├── system_controller.py      # 系统控制器 (线程通信, 任务队列)
│   └── (缓存文件 __pycache__...)
│
├── data/                         # 数据配置层
│   ├── config_Board.ini          # 板卡配置文件
│   └── config_CDM.ini            # CDM配置文件
│
├── driver/                       # 硬件驱动层
│   ├── serial_worker.py          # 串口工作线程
│   └── (缓存文件 __pycache__...)
│
├── logs/                         # 日志输出目录
│   ├── run_20260209.log
│   └── ...
│
├── resources/                    # 资源文件 (UI设计源文件)
│   └── UI/
│       ├── main_window.ui
│       └── ...
│
└── utils/                        # 界面逻辑层
    ├── __init__.py
    ├── main_cb.py                # 主界面逻辑 (Signal/Slot, 业务流程)
    ├── Ui_main_window.py         # 编译后的UI布局文件
    └── (缓存文件 __pycache__...)




    
    def _handle_incoming_data(self, raw_line):
        source, content = Protocol.parse_line(raw_line)


        
        if not source and raw_line.strip():
            source = "RAW"
            content = raw_line.strip()

        if not source: return


#        === [新增] 大块波形数据内存拦截器 (RAM Interceptor) ===
        if "----BEGIN_" in content:
            if "BEGIN_DATA" in content:
                self.bulk_mode = "P22"
            elif "BEGIN_NNQ_" in content:
                ch = content.split("_")[-1].strip("-")
                self.bulk_mode = f"NNQ_{ch}"
            elif "BEGIN_SCAN_CURVE_CH" in content:
                ch = content.split("CH")[-1].strip("-")
                self.bulk_mode = f"SCAN_{ch}"
            
            self.bulk_buffer = []  # 清空内存池
            return

        if "----END_" in content:
            if "END_DATA" in content:
                # 单个大数组接收完毕，抛给 GUI
                self.sig_bulk_data_ready.emit("P22", self.bulk_buffer)
                
            elif "END_NNQ_" in content:
                ch = self.bulk_mode.split("_")[-1]
                self.bulk_dict[ch] = self.bulk_buffer
                if len(self.bulk_dict) == 6:  # 6个通道收齐
                    self.sig_bulk_data_ready.emit("NNQ", self.bulk_dict)
                    self.bulk_dict = {}
                    
            elif "END_SCAN_CURVE" in content:
                ch = self.bulk_mode.split("_")[-1]
                self.bulk_dict[ch] = self.bulk_buffer
                if len(self.bulk_dict) == 6:
                    self.sig_bulk_data_ready.emit("SCAN", self.bulk_dict)
                    self.bulk_dict = {}
                    
            self.bulk_mode = None
            return

        # 如果正处于大块数据接收状态，把纯数字吃进内存池
        if self.bulk_mode is not None:
            try:
                # 处理可能一行有多个被逗号/空格分隔的数字
                parts = content.replace(',', ' ').split()
                for p in parts:
                    self.bulk_buffer.append(float(p))
            except ValueError:
                pass  # 过滤掉偶尔混入的非数字字符
            return




        # === 1. 解析 STM32 ADC Power 数据 ===
        # 格式: "ADC_HEATER_VCC : 2704"
        if "ADC_" in content and ":" in content:
            adc_match = re.match(r"(ADC_\w+)\s*:\s*(\d+)", content)
            if adc_match:
                # [新增] 必须先发射Info信号，确保在Log窗口能看到 "ADC_xxx: yyy"
                self.sig_stm_info.emit(content)
                
                key = adc_match.group(1)
                val = int(adc_match.group(2))
                self._stm_adc_buffer[key] = val
                self.stm_batch_timer.start()
                return

        # === 2. 解析 STM32 DAC 读取返回 ===
        # 针对: "ACK:DAC 1=1613 2=372 3=1240" (包含在ACK行中，需在ACK Return前解析)
        if "DAC" in content:
            pairs = re.findall(r"(\d+)\s*=\s*(\d+)", content)
            if pairs:
                for ch_str, val_str in pairs:
                    try:
                        self.sig_stm_dac_read.emit(int(ch_str), int(val_str))
                    except: pass
            
        # === [修改点] 解析 410 Dither 读取返回 ===
        # 格式: "Resp: DITHER XP_I 15"
        if "Resp: DITHER" in content:
            match = re.search(r"DITHER\s+(\w+)\s+(\d+)", content)
            if match:
                ch_name = match.group(1)
                val = int(match.group(2))
                # 触发信号给 UI 更新文本框
                self.sig_dither_read.emit(ch_name, val)
                # 发送给文本框显示
                self.sig_stm_info.emit(f"410: {content}")
                # 【修改点：删除这里的 return！让 log 正常打印】

        # === 3. ACK 检查 ===
        is_ack = False
        upper_c = content.upper()
        if "ACK" in upper_c or "POWER ALL" in upper_c:
            is_ack = True
            if "ADC POWER" in upper_c:
                self._stm_adc_buffer.clear()

        if is_ack:

            log.info(f"<< RX Get\n{content}")

            if self.is_waiting_ack:
                self.ack_timeout_timer.stop()
                self.is_waiting_ack = False
                if self.current_task and self.current_task.callback:
                    self.current_task.callback(True)
                self.current_task = None
            return

        # === 4. 数据分发 ===
        if source == "STM":
            self.sig_stm_ack.emit(content)
            self.sig_stm_info.emit(content)

        elif source == "410":
            self._parse_410_data(content)
            
        else:
            self.sig_stm_info.emit(f"RAW: {content}")

